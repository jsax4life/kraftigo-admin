export default function SettingsPage() {
  return (
    <div>
      <h1 className="text-lg font-semibold">Settings</h1>
      <p className="text-sm text-slate-400">
        Admin configuration for roles, permissions, and platform settings.
      </p>
    </div>
  );
}

